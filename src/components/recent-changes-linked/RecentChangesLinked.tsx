import React from 'react';
import { useDispatch } from 'react-redux';

const RecentChangesLinked = () => {
  const dispatch = useDispatch();
  React.useEffect(() => {}, []);
  return <>dfdfdf</>;
};

export default RecentChangesLinked;
